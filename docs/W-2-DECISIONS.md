# W-2 · v3.0.60 — what the session decided while the Arbiter was away

Why this file exists: `docs/PLAN-W-2.md`, *Run without the Arbiter*. Each line gives what was decided,
where (commit or file), and why. A decision that turns out to be wrong is reverted by his word, and
the line gets a note saying so. Nothing here was irreversible: the merge and the tag wait for him.

| # | package | decision | why | where |
|---|---|---|---|---|
| 1 | R | **The layout of the per-project version line.** `state/versions/v_NNN.json`, plus `state/slots.json` (it replaces `registry.json` and every `<preset>/HEAD`), plus `state/legacy/<preset>/` and `state/legacy-map.json`. A version keeps `preset` (the slot it was banked for) and gains `parent` | one line of numbers, and one file saying what each slot holds, which is the Arbiter's model (a preset is the slot a version is fixed in). No new field for what `preset` already says | hub `#195`, the reader-contract comment |
| 2 | R | **At migration the active slot keeps its numbers**, and the other presets are numbered after it | the Passat's journal cites `v_0NN` with no preset, and they are almost all SQ's (the active one); keeping SQ's numbers keeps those citations true without rewriting history. `legacy-map.json` records every move | same |
| 3 | R | **Both layouts are read. The migration is offered with the user's OK, never automatic. A half-migrated project is refused** | "one-way and refusing" (`#195` ask 3) without breaking anything that exists. So it is not `### Breaking`, and the wave stays a patch (the Arbiter: no minor is cut, `WAVES.md` §3.1). The schema number stays 3, because the content of a version did not change | same |
| 4 | A | **`hardware.controls` holds remote knobs only.** A name the project's profile lists as a feature is refused unless it is `…RC` or `Remote…`, so `RemoteToneControl` counts as a knob (the Conductor's tone mode). A name the profile does not know goes in only with `source=user` | S-025: `RealCenter` and `VirtualX` sat there as if a person turned them. The Arbiter said the Conductor is one knob whose modes cycle volume → sub → rear → tone, so its remote modes are knobs and the software features are not. A person may still name a knob of their own (the head unit's bass), which the form's «+ додати свій» already does | `project.py` `set_hardware_control` |
| 5 | A | **`sources` lines are still scanned for paths, but only a literal path counts.** A token with `...`, `…`, `{}`, `*` or `?` is a citation and is not checked. The plan said "never checked as a path", and it was narrowed to this | S-027's false "gone" came from `.../{README.md`. Dropping the scan altogether would also lose skill #36's real catch: `hand-copied from Z:/dev/…/resonalyze-passat`, a path on another machine | `contract.py` `provenance`, `_NOT_LITERAL` |
| 6 | A | **An underscore id is flagged even when a rename stands behind it, and it becomes its hyphen form.** An id outside the notation and outside `previous_names` becomes the code. An id the ledger keys rows by while the target keys none is held, not changed | the Passat's `w_L` … `tw_R` are in `previous_names` too (read 2026-09-22), so "not a previous name" alone would have missed them. The Arbiter's rule is the notation, everywhere. Holding the orphan case keeps `fix-ids` from breaking a ledger | `project.py` `id_mismatches` / `fix_ids` |
| 7 | A | **`contract.py check` shows the id fix as a block of its own, not as a gate failure** | the session reads that report at every start, so this is where it learns to ask. A notation fault that breaks nothing by itself should not stop Phase 0 | `contract.py` `render_report` |
| 8 | B | **Inside an agent session the reviewer CLI RUNS**, with the session's markers stripped from its environment and a bounded, announced wait. This **reverses** a refusal that stood since hub TCC-014: field sessions hung (~15 of 20) and a silent wait read as thinking | measured on 2026-09-22 from inside this session: `agy` answered in 4 s with the markers stripped and in 27 s and 48 s with them kept. TCC-024 (hub #187) saw the same by hand. A TCC-launched session has no separate terminal (#54), so the refusal left it only the clipboard. What hung was a wait with no end and no word; the wait is now bounded and named | `autosound_ai.py` `child_env`, `main` |
| 9 | B | **The transport follows the model.** An id ending in an agy tier (`-high`/`-medium`/`-low`/`-minimal`) goes to the CLI even when a key is exported, and a 404 on a name the CLI lists falls through to it. The Arbiter's 09-08 rule stands for a name nobody serves: a 404 there is still a choice (exit 3) | hub #187 ask 1: the same TCC settings gave a review in one shell and a 404 in another, depending only on an exported key | same |
| 10 | B | **`--via api` uses the key from the environment even where `critic-env` blanks it**, for that run only, and calls the API with the tier dropped (`gemini-3.8-flash-high` → `gemini-3.8-flash`) | #55 ask 3: the choice depends on where the run happens, and one machine-wide switch cannot express that. The blank stays the machine's default | same |
| 11 | B | **The route is reported as its own line, `>> REVIEW_ROUTE: api|cli` on stderr**, and the `— [critic: <model>]` marker is left unchanged | TCC's `core/critic.py` parses that marker, so a changed marker would break the paired release | same |
| 12 | B | **An exported key no longer tells the installer that the reviewer is set up.** When a key is exported and there is no sign-in, the installer says that agy does not use the key, and that the key belongs in `critic-env`, not the shell profile | hub #187 (the comment of 2026-09-19): signal 5 hid a missing login and skipped the sign-in | `install.sh` `agy_status`, `install.ps1` `Get-AgyStatus` |
