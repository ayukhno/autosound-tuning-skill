# W-2 · v3.0.60 — what the session decided while the Arbiter was away

Why this file exists: `docs/PLAN-W-2.md`, *Run without the Arbiter*. Each line gives what was decided,
where (commit or file), and why. A decision that turns out to be wrong is reverted by his word, and
the line gets a note saying so. Nothing here was irreversible: the merge and the tag wait for him.

| # | package | decision | why | where |
|---|---|---|---|---|
| 1 | R | **The layout of the per-project version line.** `state/versions/v_NNN.json`, plus `state/slots.json` (it replaces `registry.json` and every `<preset>/HEAD`), plus `state/legacy/<preset>/` and `state/legacy-map.json`. A version keeps `preset` (the slot it was banked for) and gains `parent` | one line of numbers, and one file saying what each slot holds, which is the Arbiter's model (a preset is the slot a version is fixed in). No new field for what `preset` already says. **Extended by the Arbiter, 2026-09-23:** a `v_NNN` is a proposed DSP state, and what he saves into a device preset gets a name of his own (SQ-1, FULL-v1, SQ-2). The name is recorded with `state.py config save`, and the comparison takes the PREVIOUS configuration up the `parent` line (SQ-2 = v_006 against SQ-1 = v_003, never v_005). TCC's half is hub #198 | hub `#195`, the reader-contract comment; `state.py` `save_config`, `previous_config`, `compare_config` |
| 2 | R | **At migration the active slot keeps its numbers**, and the other presets are numbered after it | the Passat's journal cites `v_0NN` with no preset, and they are almost all SQ's (the active one); keeping SQ's numbers keeps those citations true without rewriting history. `legacy-map.json` records every move | same |
| 3 | R | **Both layouts are read. The migration is offered with the user's OK, never automatic. A half-migrated project is refused** | "one-way and refusing" (`#195` ask 3) without breaking anything that exists. So it is not `### Breaking`, and the wave stays a patch (the Arbiter: no minor is cut, `WAVES.md` §3.1). The schema number stays 3, because the content of a version did not change. **Accepted by the Arbiter, 2026-09-23** | same |
| 4 | A | **`hardware.controls` holds remote knobs only.** A name the project's profile lists as a feature is refused unless it is `…RC` or `Remote…`, so `RemoteToneControl` counts as a knob (the Conductor's tone mode). A name the profile does not know goes in only with `source=user` | S-025: `RealCenter` and `VirtualX` sat there as if a person turned them. The Arbiter said the Conductor is one knob whose modes cycle volume → sub → rear → tone, so its remote modes are knobs and the software features are not. A person may still name a knob of their own (the head unit's bass), which the form's «+ додати свій» already does. **Revised by the Arbiter, 2026-09-23:** there are no universal controls (even the sub may be two), so the list is his. A new name goes in only on his word. A session that meets something new asks him, and the refusal says so. A name on his list is tracked from any source. The RC/Remote rule is gone | `project.py` `set_hardware_control` |
| 5 | A | **`sources` lines are still scanned for paths, but only a literal path counts.** A token with `...`, `…`, `{}`, `*` or `?` is a citation and is not checked. The plan said "never checked as a path", and it was narrowed to this | S-027's false "gone" came from `.../{README.md`. Dropping the scan altogether would also lose skill #36's real catch: `hand-copied from Z:/dev/…/resonalyze-passat`, a path on another machine. **Accepted by the Arbiter, 2026-09-23, with an addition:** a path that is not found says where to look for it. The report names what cited it (`sources[N]`, `seeded_from.path`) and what its shape tells: a Windows drive written on another machine, an unmounted volume, another user's home, or moved here, with the name to search for | `contract.py` `provenance`, `_NOT_LITERAL`, `where_to_look` |
| 6 | A | **An underscore id is flagged even when a rename stands behind it, and it becomes its hyphen form.** An id outside the notation and outside `previous_names` becomes the code. An id the ledger keys rows by while the target keys none is held, not changed **Accepted by the Arbiter, 2026-09-23.** | the Passat's `w_L` … `tw_R` are in `previous_names` too (read 2026-09-22), so "not a previous name" alone would have missed them. The Arbiter's rule is the notation, everywhere. Holding the orphan case keeps `fix-ids` from breaking a ledger | `project.py` `id_mismatches` / `fix_ids` |
| 7 | A | **`contract.py check` shows the id fix as a block of its own, not as a gate failure** **Accepted by the Arbiter, 2026-09-23.** | the session reads that report at every start, so this is where it learns to ask. A notation fault that breaks nothing by itself should not stop Phase 0 | `contract.py` `render_report` |
| 8 | B | **Inside an agent session the reviewer CLI RUNS**, with the session's markers stripped from its environment and a bounded, announced wait. This **reverses** a refusal that stood since hub TCC-014: field sessions hung (~15 of 20) and a silent wait read as thinking | measured on 2026-09-22 from inside this session: `agy` answered in 4 s with the markers stripped and in 27 s and 48 s with them kept. TCC-024 (hub #187) saw the same by hand. A TCC-launched session has no separate terminal (#54), so the refusal left it only the clipboard. What hung was a wait with no end and no word; the wait is now bounded and named. **Amended by the Arbiter, 2026-09-23** («300 с мало, бо вчора переклад польською відвалився»): the 22.09 translations were 31 KB and took 170–370 s, so the wait now grows with the job, 600 s at least and 25 s per KB (`cli_wait`). His second point was the key that `~/.zshrc` does not hand to a GUI-started session, and that is unsafe there. It is researched (`docs/RESEARCH-2026-09-23-reviewer-keys.md`) and, on his choice, **built in W-2**. `key set` stores the key through a hidden prompt or stdin, never argv. It goes into the macOS Keychain (`security -i`, the key on stdin) or into Windows DPAPI, with the 600 file as the fallback. The read order is file, then keystore, then environment. That order lets a stale export lose, and a file TCC still writes wins without drift. `key move-shell` asks first. TCC's half is hub #197 (SKL-051). DPAPI is checked on CI's Windows runner, because the VM cannot be driven from here | `autosound_ai.py` `child_env`, `main`, `cli_wait`, `key_*`, `move_shell`; `checks.yml` |
| 9 | B | **The transport follows the model.** An id ending in an agy tier (`-high`/`-medium`/`-low`/`-minimal`) goes to the CLI even when a key is exported, and a 404 on a name the CLI lists falls through to it. The Arbiter's 09-08 rule stands for a name nobody serves: a 404 there is still a choice (exit 3) **Accepted by the Arbiter, 2026-09-23** (with no API key the CLI is used anyway; this row is only the key-present case). | hub #187 ask 1: the same TCC settings gave a review in one shell and a 404 in another, depending only on an exported key | same |
| 10 | B | **`--via api` uses the key from the environment even where `critic-env` blanks it**, for that run only, and calls the API with the tier dropped (`gemini-3.8-flash-high` → `gemini-3.8-flash`) **Accepted by the Arbiter, 2026-09-23.** | #55 ask 3: the choice depends on where the run happens, and one machine-wide switch cannot express that. The blank stays the machine's default | same |
| 11 | B | **The route is reported as its own line, `>> REVIEW_ROUTE: api|cli` on stderr**, and the `— [critic: <model>]` marker is left unchanged **Accepted by the Arbiter, 2026-09-23.** | TCC's `core/critic.py` parses that marker, so a changed marker would break the paired release | same |
| 12 | B | **An exported key no longer tells the installer that the reviewer is set up.** When a key is exported and there is no sign-in, the installer says that agy does not use the key, and that the key belongs in `critic-env`, not the shell profile **Superseded on 2026-09-23:** the installers now point to `autosound_ai.py key move-shell` (the keystore, row 8). | hub #187 (the comment of 2026-09-19): signal 5 hid a missing login and skipped the sign-in | `install.sh` `agy_status`, `install.ps1` `Get-AgyStatus` |
| 13 | R | **A new project starts on the per-project line**. An empty `state/` gets `slots.json` before its first version **Accepted by the Arbiter, 2026-09-23.** | the Arbiter's model is the default from now on. Old projects move only on his OK | `state.py` `ledger_layout`, `snapshot` |
| 14 | R | **The change sheets move to `state/proposals/`** on the new line **Accepted by the Arbiter, 2026-09-23.** | a directory named after a preset would look like a per-preset ledger half-way through | `apply.py` `write_delta`, `PresetHistory.proposals_dir` |
| 15 | R | **A variant is a version on the line with `variant: <name>`, banked without moving the slot**. A switch moves a pointer and copies nothing **Accepted by the Arbiter, 2026-09-23.** | #58 P2: a project-local script copied variant files over a banked `v_011` three times | `state.py` `variant new/list/switch`, `PresetHistory.place` |
| 16 | R | **`slots.json` beside old per-preset directories is refused and never rebuilt over.** Only `versions/` without `slots.json` resumes **Accepted by the Arbiter, 2026-09-23.** | the first is a person putting old files back, and which side is newer is not a thing to guess. The second is this tool's own stopped move | `ledger_layout`, `migrate_line` |
| 17 | F | **The junction level step is each block's own-band level plus its gain, lower minus upper.** It comes from the engine's `bandLevelDb` and the configuration's gains, not from a new engine field **Accepted by the Arbiter, 2026-09-23**; the shared-band reading is a candidate for the next wave (TODO S-056). | #50 asked for "the dB difference over the shared band". Two members sum flat when their plateaus match, and that is one subtraction on numbers the engine already reports. A shared-band reading would need a rebuilt engine and new goldens | `resonalyze_engine.py` `level_steps` |
| 18 | F | ~~The gain-structure finding moves the quiet channel's amplifier~~ **REVISED by the Arbiter, 2026-09-23: levels are always the DSP's.** Above a 3 dB cut-only spread the quiet channel is lifted in the DSP by the smallest cut among the others, up to the profile's `channel_gain.range_db` top. Only what that range cannot reach is the amp's, and it is his call. An amp gain he turns is recorded per channel (`process.py amp-gain`), and `verify_prediction` takes it out of a comparison across it, beside the knobs | his words: amps are set before the tune and move only when the DSP's plus runs out; then he tells the session, and the re-measure knows which channels changed. The Passat's table is +4.8 dB on the sub, inside the Helix's +5, so no amp at all | `level_offsets.py` `dsp_level_plan`; `process.py` `record_amp_gain`/`amp_gain_between`; `verify_prediction.py` `with_amp` |
| 19 | F | **The bundled Helix profile carries the step OPTIONS and no step.** A project records its machine's setting through `dsp_profile.py set-setting`, which accepts only the paths in `MACHINE_SETTINGS` and only a value the profile lists **Accepted by the Arbiter, 2026-09-23.** | #52: a setting was recorded as a constant, and a bundled profile is shared by every machine. A step that nobody asked is left unknown, and `apply.propose` asks for it, rather than one machine's value being carried to all | the Helix profile, `dsp_profile.py`, `apply.py` |
| 20 | F | **A trim off the recorded step is an ADVISORY, not a refusal** **Accepted by the Arbiter, 2026-09-23.** | the number is right, and the machine may simply be set coarser, which is the tuner's switch. What was missing was saying so, not a gate | `apply.py` `gain_grid_advisories` |
| 21 | V | **The candidates are the best, each wish as a whole variant, and with no wish the engine's top 3 alternatives.** An edge under a limit in the pool is moved to the nearest allowed and named **Amended by the Arbiter, 2026-09-23:** a moved edge is re-computed, not just moved. The junction beside it is re-tuned inside the limits, in the same family and slope, and Auto delay runs after it. | #38 asks for variants that win under different weightings. The engine's ranked pool is the ready source, but it is ranked on sound alone, and on the synthetic set two of its alternatives put a tweeter under its floor | `resonalyze_engine.py` `alternative_variants` |
| 22 | V | **Four terms, and three weightings that only pick.** The weights (1 / 0.3) are never shown as a score, and the picks keep the candidates' order **Amended by the Arbiter, 2026-09-23** (his rule of 17.09: show, describe, ADVISE): the front advises from `goal.purpose`. Competition gets the stage-first pick, for yourself the tone-first pick, and both one per preset. With no goal it says to ask. The choice stays his. | `ladder_report`'s rule: a table sorted by the metric is a proposal dressed as a table. #38 asked for a front, not a winner | `variant_front.py` |
| 23 | V | **A spread under a tie margin is a tie, not a trade** (tonal 0.1, stage 0.2, junctions 0.05, ripple 0.1 dB) **Amended by the Arbiter, 2026-09-23:** when candidates are close, say so. Candidates within the tie margins on every term are named as close, worth taking both to the car to listen. | on the smoke set the alternatives differed by 0.01–0.05 dB, which the first version reported as "buys the best junctions": the arithmetic's precision sold as a property of the car | `variant_front.TIE_DB` |
| 24 | V | **The terms come from the set's solos as recorded**; a protective filter captured in them is not divided out, and the note says so **Reversed by the Arbiter, 2026-09-23:** «if the mid's protective filter is not accounted for, at the junction with the midbass we see its phase wrong». He was right. The filter's phase at one corner is not its phase at another, so candidates were read through different errors. The engine divides it out, and now so do the front's terms (`predict.de_embed_solos`). The smoke marks a protective high-pass on the mids and checks it is taken out. | every candidate carries the same solos, so the comparison holds even where the absolute numbers carry the filter. Dividing it out means the capture round's protective record, which is E's work (#57 P0) | `resonalyze_engine.candidate_terms` |
| 25 | C | **A git or python3 that is present but does not run stops the installer (git) or is named (python3)**, with what it printed and `brew install git` / `brew install python` **Extended by the Arbiter, 2026-09-23, with hub #199 (TCC-026):** GitHub wanted means GitHub set up. The installer runs `gh auth setup-git` after the sign-in. A project is a repository in code: `project_seed` calls `project_repo.init`, and `contract.py check` names the init, or the private backup for the user's yes. | hub #192: `xcode-select -p` answered on a machine whose only git died on `xcrun`, and the install went on until a clone failed | `install.sh` `runs_ok`, `broken_tool` |
| 26 | C | **The receipt is `install-receipt.json` beside the engines** (`~/.local/share/autosound/` or `%LOCALAPPDATA%\autosound\`), carrying the installer's own sha256 **Accepted by the Arbiter, 2026-09-23.** | S-049: an old bookmarked `install.sh` installs old logic while the method updates to the newest tag. The installer's own digest is what tells two installers apart | both installers, `autosound_ai.receipt_line` |
| 27 | C | **A prebuilt engine's wrapper is known only when the installer fetched the tag it had just installed**; a hand fetch of another tag, or a hand-installed zip, records no digest and is not judged **Extended by the Arbiter, 2026-09-23:** the engine is installed with the skill and checked. `resonalyze_engine.py check` runs one Auto crossover on a tiny synthetic set in seconds. The installers run it after a fetch, and where an SDK is present they build the engine at install (`check --build`) instead of on first use. | the archive carries no wrapper digest of its own, and adding one would change CI's release job, which is the hub's release channel. The installer's case is the ordinary one | `resonalyze_engine.py` `fetch_binary`, `prebuilt_mismatch` |
| 28 | D | **Only `hardware.controls` stays behind; the rest of `hardware` travels** (`virtual_routing`, `control_mapping`, `description`) **Revised by the Arbiter, 2026-09-23:** controls travel too. The list is his, and each control arrives marked as carried in, so `contract.py check` asks him to confirm or change it. | #185 names the control module's STATE. The routing matrix and what a step of a knob is worth are facts about the install, and the new car has the same ones | `project_seed.py` |
| 29 | D | **`~/…` travels; `/…`, `\\…` and drive paths do not** | #186's case was `/Users/o.yukhno/…` on another MacBook. A home-relative path means the same place on any machine | `project_seed._absolute` |
| 30 | D | **The protective history is the source's last capture round's protective record**, read through `process.py`, not the prose note (`paths.irs_49_note`) **Revised by the Arbiter, 2026-09-23:** not the last round. The history is gathered per channel, from the newest round where that channel had a protective filter (`Process.protective_by_channel`). The front may be taken raw in one round and the centre in another; both are kept, and an OFF hides nothing. It used to read only a round still open. | the record is a field, and the prose note is exactly what #185 said was neither a setting nor an honest note | `project_seed._protective_history` |
| 31 | E | **A seal is the digest of a version's CONTENT (canonical JSON), not its bytes**, kept in `state/seals.json` and written at banking. A seal is never renewed; `seal` only seals the unsealed **Accepted by the Arbiter, 2026-09-23.** | #58 P1. The encoding repair (TCC-007) and a line-ending change rewrite bytes without changing a version; renewing a seal would bless the very mutation it exists to catch | `state.py` `seal_all`, `verify_seals`; `contract.py` |
| 32 | E | **No third protective answer.** #56's comment proposed a record value "measured as configured". The Arbiter ruled on 2026-09-06 "two answers, not three": OFF or the legs, and an absent record MEANS "as configured". The defect was `eq_propose` claiming `baseline=True` for every round, which e3 fixes by taking it from the part/phase **Accepted by the Arbiter, 2026-09-23.** | a new value would reverse a recorded ruling for a bug that lives elsewhere | `eq_propose.py` (e3); `process.py` unchanged there |
| 33 | E | ~~A series round binds `under` to the active slot's version when none is given~~ **REVERTED the same night: `under` is recorded only when given, and must be banked.** The full suite caught it: a baseline taken on the bare `v0` preset was bound to the slot's chain, and `predict` divided by a chain the measurement never went through (−240 dB where the file read −31 dB) | #57 P0: the round is the one record, and the configuration in the processor while measuring is part of it. The default is the slot the DSP holds; a person who knows otherwise passes `--under` | `process.py` `start_capture`, `under_for` |
| 34 | E | **Structural = a polarity, a crossover edge, a delay ≥ 0.5 ms, a gain ≥ 6 dB.** Such a move banks in the slot only with evidence that resolves AND one review file; otherwise it is a 🟠 candidate on the line, not in the slot, never attested. An import of what the device holds (`provenance`) is exempt | #57 P2 ("above an advisory threshold"; the gain one is the gate's own 6 dB) and #58 P7 ("reviewer at banking a structural change only") together. A candidate is a version, not a refusal, so nothing the desk proposes is lost, and it is fast (#58 P11). A desk's fine delay trim stays cheap below 0.5 ms | `apply.py` `structural_moves`, `evidence_problems`, `propose` |
| 35 | E | **The verification set is derived from the diff**: EQ or gain → that channel's solo `(rta)`; a delay or an edge → the junction's pair `(sw)` plus a moved edge's solo `(rta)`; a polarity → the pair as MMM, normal and inverted. The junction partner is the facing edge within 15 % on the same side | #57 P3's own table. The ledger knows the edges, so the partner is read from them, not typed | `state.py` `verification_set` |
| 36 | E | **#58 P5, P8, P10 and P12 are rules in SKILL.md's Output Style; P6 is an advisory in `apply.propose`**: an edge that moved while its delay stayed is named | P5, P8, P10 and P12 are behaviour a session carries in every reply, and SKILL.md is the always-loaded core. P6's "values that depend on a changed one" has one mechanical case the gate can see, the carried delay | `SKILL.md`, `apply.advisories` |
| 37 | E | **`capture-import` opens one round per DSP state, bound by the title's MODIFIER** (`--bind C=v_013`; the plain title is `--bind =v_012`). An unbound modifier, a title of another series, or missing knobs refuses the whole import. A late registration is closed at once with its reason on the round | #58 P3: 21 titles covering two DSP states in one round, knobs ignored, a round invented to get past the foreign-series refusal. The modifier is where the session's own titles carried the variant | `process.py` `capture_import` |
| 38 | E (agent e1) | **#56 item 2 was half wrong**: `excess_phase_version` already sent REW's four lower-case keys in every version (`git log -S`), so the quoted 400 came from a direct call. What was true, and is fixed: the function never checked that the `-EP` measurement appeared. Now it waits up to 20 s by uuid and raises with REW's answer | the evidence in the repo's own history outranks the report | `rew_api._create_version` |
| 39 | E (agent e1) | **The min-phase verdict tests the residual's RMS AND its maximum** (10° / 30°, provisional, tuned on synthetic data). **Settled by analysis, 2026-09-23** (the Arbiter: research it, don't ask; `docs/RESEARCH-2026-09-23-min-phase-verdict.md`): the criterion is sourced and the number is the measurement's. The window is the feature's own ±1/6 oct. The CHANNEL's delay is held fixed (`channel_delay_ms`). The threshold is max(10°, 3 × the repeat floor), with max ≤ 3 × that. The floor comes from the centre returns (`min_phase_across`), and 2 of 3 must agree, else UNSTABLE. Without repeats the verdict is PROVISIONAL. It is decisive below 200 Hz and corroborating above. A read coarser than 1/48 is refused | no source publishes a threshold in degrees or ms (REW help, Mulcahy, Toole 2015, Radlović ×2, Brännmark & Ahlén, DRC). They give flat excess GD after the delay (REW; Neely & Allen 1979), the Schroeder split, and stability across positions. In the probes, ±0.5 oct charged a min-phase peak with a notch 0.4 oct away (52° rms). 1/24 smoothing turned r = 1.05 into MIN. 10° of read noise flipped a true peak. `flaw_map` no longer calls a peak "minimum-phase at the gate", because the gate judges dips. **Waits for real data** (S-055): the blanket `cabin_null` below Schroeder in `flaw_map` against §13, and the Passat check | `eq_gate.min_phase_verdict`, `min_phase_across`, `channel_delay_ms`; `flaw_map` |
| 40 | E (agent e2) | **Per-band targets use one file name (`<code>_target.txt`) and codes from the glossary; the retired NTT spelling is named, not read.** A missing `hpf_type` still defaults to `lr4`. **Settled by the Arbiter, 2026-09-23: LR24 is always a good base, and when the tuner set no type the alternatives get a look.** The engine already searches every family the device has; `resonalyze_engine run` now also builds an LR24 base (every junction re-tuned in LR24 alone around the best's corner, Auto delay over the chain) and puts it on the trade-off front beside the best and the ranked alternatives | a target's default is the base he named, so it stays. The look at alternatives was already the engine's search. What was missing was the base on the same front, so the best is read against a known-good configuration and not only against its own runners-up. A wish with a family or a slope counts as a set type; a corner alone does not | `target_bands`; `resonalyze_engine.base_type_variant`, `type_was_set` |
| 41 | E (agent e4) | **A junction's pair is looked for as `(sw)` first, then `(rta)`; a wider pair (`SW+Ws`) is never attached automatically; `--no-pair` still attaches a pair it finds; the Phase-1 baseline docs add `--no-pair`** | the solos are swept point readings, and so is the pair that verified on the Passat (`R m+tw_50 (sw)`). A wider pair is not the sum of the two solos. The `_1` baseline has no pairs. The method's docs disagree on pairs (phase_2 says MMM, virtual-first 3.2 says sweep): a question for the Arbiter, and one constant (`PAIR_METHODS`) flips it | `rew_tool.analyze_joints` |
| 42 | E | **The foreign-file check reads `containingFileName` (and two other spellings) defensively, and LABELS in `contract.py check`**. Refusing in the solo loaders follows once `predict`/`eq_propose` are merged | the field name comes from the 2026-09-22 review and could not be seen here: REW was open with no measurements. A record without a file name answers "REW did not say", never "ours" | `rew_api.measurement_file`, `foreign_measurements`; `contract.cross_check_rew` |
| 43 | E (agent e3) | **`eq_propose --part 2` reads magnitude from `(rta)` and excess phase from `(sw)`, and refuses a series without the `(rta)`. A band is held to where its channel plays: within 20 dB of its passband level, the margin `_live` already uses. `baseline` follows the part and the round's phase** | #56 items 7 and 8. The 20 dB margin drops both of the issue's cases (a tweeter's 845 Hz is 28 dB down). The part-2 gate is loaded but consulted by no broad cut yet: said, not faked | `eq_propose.py`, `predict.load_rta_rew` |
| 44 | E | **`predict --rew` defaults `--from-state` to the round's `under`** (`none` opts out), and refuses solos from another `.mdat` (`--allow-foreign`) | #56's third comment asked for exactly this default. It is the round-record rule of #57 P0 reaching the tool that needed it most | `predict.py` `main` |
| 45 | E | **#57 P5 (compact output) is done where the issue measured it: `predict`'s notes and the missing-title error.** Notes collapse by text with their channels joined, at most five show unless `--verbose`, and the JSON keeps all of them. The missing-title error gives the count and the three nearest titles | the issue's three examples were eight identical "solo used as recorded" notes, the whole REW title list on every missing channel, and one paragraph printed twice. A five-line verdict block for every tool is a redesign, not a patch, so it is named here rather than half-done | `predict.compact_notes`, `rew_api.find_measurement_id` |
| 46 | E | **The Arbiter, 2026-09-23: a DESK result is evidence** («у нас є математика своя і Resonalyze … підходи до машини мінімальні на перших етапах»). A `predict`/`resonalyze_engine` output file banks a structural change in the slot, with one review. A candidate appears only when there is not even a computation, and its sheet says to compute, not to measure. `predict` keeps taking the version from the round, since that is what keeps the desk's computation right without a car | the car verifies several changes in one visit (`verification-set <last verified> <now>`). Asked and answered: rows 34 and 44 stand as amended here | `apply.evidence_problems`, the candidate sheet |
