# Helix DSP Ultra S — filled-in DSP checklist (intake §4)

Ready answers to the capability checklist `project-intake.md §4`. Detail — in the three references: `helix-vcp-workflow.md` (architecture/gain/AKM) · `helix-eq-export.md` (EQ exchange) · `helix-phase-allpass.md` (phase).

| Question | Helix DSP Ultra S |
|---|---|
| Slot counts per tier (`max_count`) | **Outputs: 12 (A–L). Virtual: 8 (A–H).** The processor's own counts, not one car's — the point is the spares: a build using outputs B–K has A and L free, and they belong in `project.json` as `hidden`/`role: unused` rows carrying `tier` (SCR-042). ⚠️ **Slot letters repeat between the two tiers**, so a slot letter alone never identifies a channel — `F` is a legal address in both |
| Processing layers | **2: VIRTUAL (groups Front L/R, Center, Rear **and SUB** — a virtual sub channel exists, user-verified 2026-07; an earlier "no virtual sub" note was wrong; Link L+R) + OUTPUT** (per-channel) → the base+voicing architecture works fully; RealCenter pulls its signal from the virtual layer |
| EQ | **30 bands/channel**, types PK / LS_Q / HS_Q + **AP1/AP2** (all-pass, several allowed). **Filter limits (user-verified in PC-Tool, 2026-08-22): Q 0.5–50; in Fine EQ mode Q 0.5–15 and the frequency step is 1 Hz. Frequency range 10–40000 Hz (2026-08-23) — note it runs WIDER than the crossover's 20–20480, so the two must not share one profile field.** ✅ **RULE (the user, 2026-08-23): record the FULL range everywhere, because it can be entered by hand.** Settled first on the sub's crossover — the UI offers 20–300 Hz and a higher corner types in fine — and then generalised: *«кругом беремо повний діапазон, раз руками можна задати»*. So where a UI MODE offers a subset, the profile carries the superset. That resolves what looked like a three-file contradiction: `sound_AutoSci`'s YAML and the live Passat's JSON hold step `0.01` with Q `0.5–15`, `autosound-tcc`'s holds step `1.0` with Q `0.5–50` — two coherent MODE readings, neither of them the hardware. **Record the union: Q `0.5–50`, frequency step `0.01` Hz, frequency range `10–40000` Hz.** ✅ **OBSERVED 2026-08-23, and the derived label is retired because the requirement was MET.** The 'Parametric EQ' panel was photographed in one state with every field visible together: **Freq 26.01 Hz · Gain 0.1 dB · Q 50**. That settles both halves at once — the frequency field ACCEPTS two decimals rather than merely displaying them, which was the open reservation, and Q reaches 50 in that same panel — so the pair recorded here co-occurs in ONE mode and is no longer an inference. Fine EQ is the other mode: a 1 Hz step with Q 0.5–15. The earlier requirement that anything copied from this line carry `derived from the user's full-range ruling` is **satisfied, not waived** — worth saying in those words, because a requirement quietly abandoned looks identical afterwards to one that was met. Still NOT established by that panel: the BOTTOM of the Q range (0.5 comes from elsewhere), and whether a third mode exists with limits of its own — which no longer matters, since the question was only ever whether this pair exists in one mode — and note that pairing appears in none of the three files, so all three need correcting rather than one of them being picked. (The rule is stated for ranges; I extend it to STEPS by the same logic — the finer step is the more permissive one — and say so here rather than silently.) **The trade-off, named so nobody rediscovers it as a bug:** the profile now answers "enterable on this hardware", not "enterable in the mode you are in". A checker can therefore pass a Q of 30 that Fine EQ will not accept, and the tuner meets that at the PC-Tool screen and switches mode. That is the recoverable direction. The other one — refusing a setting the hardware accepts — means a valid tune is never tried, and nobody ever learns why Gain −30 … +12 dB. ⚠️ The Q ceiling is what the HARDWARE allows, not what the method should use — see the spatial-validity Q ceiling in `diagnostic §13`: a filter narrower than the features that survive spatial averaging is fitting one mic position. Q 50 exists; it is almost never the right answer in a cabin. **File import ✓** — Audiotec-Fischer format (REW Equaliser = "Audiotec-Fischer") → never enter by hand (the bank can't be seen all at once). **AP2 is hardware-verified** — first 2026-07 in the cabin (same-session single-variable A/B: sweep → flip only the APF → sweep; the free fit recovered 4386/3.82 against 4414/4.0 entered, 31° RMS over an acoustic path), then **electrically 2026-09-01 (fact 1): a free fit recovers f0 to 0.05 % and Q to 0.3 %, and the filter is the bilinear DIGITAL biquad at the processing rate** — the analogue formula is 3.6° rms / 9.5° max out at 8 kHz (`dsp_math.apf2_response`). ⚠️ **AP1's corner sits LOW and the error grows with frequency**: typed 250/1000/4000/8000 Hz land at 248.8/993.2/3936/**7611.6** (−0.5/−0.7/−1.6/**−4.9 %**) while every AP2 corner lands within 0.05 %; the shape stays a clean first-order all-pass and no simple law fits the offset — prefer AP2, and carry AP1's deviation as a known unmodelled error (`helix-phase-allpass.md` §3). ✅ **The PK bell is RBJ, CONSTANT-Q, and the typed Q is the half-gain Q** (fact 5, electrical 2026-09-02): a free fit at 1 kHz recovers f to 0.03 %, Q to 1.1 %, gain to 0.03 dB; the half-gain edges land where RBJ puts them (619.5/1610.6 Hz at Q 1 against 618.0/1618.0); a +12 dB band read at its own half-gain gives the same edges as a +6 dB one, so the width does not depend on the gain — not proportional-Q. ⚠️ **A SHELF's Q is bounded 0.3 … 2, not 0.5 … 50** — measured on the bench of 2026-09-01 (20 electrical sweeps, hub `#36`): the 50 belongs to the bell. The profile carries it as `parametric_eq.q_range_by_type`, because one range for every type both refused a legitimate shelf at Q 0.3 and passed a shelf at Q 30 the processor will not take. **The Q typed into a shelf IS the RBJ Q** — a free fit of the measurement returned 0.300 / 0.707 / 1.998 for the three entered values (residual 0.005 dB), and since 2026-09-01 `dsp_math.peq_response` uses it instead of forcing S=1 (which was a different filter everywhere except Q≈0.707: 1.86 dB rms and 4.33 dB max at Q 2). **LS_Q/HS_Q at Q=0.7071 ≡ RBJ shelf S=1** — in REW model them with its "LS Q"/"HS Q" filter types (plain REW "Low shelf"/"High shelf" is a DIFFERENT definition). Modeling a FULL channel (crossovers+EQ) in REW → the **Generic Extended** equaliser, 20 slots (the Audiotec-Fischer preset lacks crossover filter types) — push schema in `rew-api-quirks.md` |
| Crossovers | ✅ **VERIFIED AGAINST THE HARDWARE (electrical bench 2026-08-31 … 09-04, facts 8–9): LR12 · LR24 · LR36 · BW24 · BW42 · BE36 are bilinear DIGITAL biquad chains at the processing rate, LR(n) = BW(n/2) squared, Bessel normalised at −3 dB, and the chain simply multiplies** (HP 500 × LP 5000 separately vs together: 0.015 dB / 0.11°). `dsp_math.xo_response` against eight published curves: 0.04–0.09 dB and 0.19–0.48° rms on the files' 1/96-octave grid (band and mask stated per case in the selftest). At 1 kHz an analogue and a digital model are indistinguishable; at 8 kHz on this 96 kHz processor they separate (the analogue LR36 leaves 0.9–1.9 dB max where the digital one leaves 0.3) — the measurement behind binding the model to the profile's rate. BW24 at 460 Hz (hub `#32`): −0.0024 dB / +0.034° from the LR24 control in the same pass, −2.985 dB at the corner (`filter-types-car-audio.md`, Butterworth). **Polarity follows the alignment**: LR12 and LR36 need one leg inverted (LR36 nulls by 37.5 dB without it), LR24 and BW42 do not. Fixtures: `rew_tool/testdata/helix-bench/`. ⚠️ **CHEBYSHEV: enterable, NOT modellable.** An experiment was run and could not identify the mathematics (user, 2026-08-23), so the ripple stays unidentified and the filter is not DETERMINED — the hardware will accept one and we cannot predict, model or verify what it does. Deferred permanently: a closed line of enquiry, not a lookup nobody has done. `dsp_math.XO_OPTIONS` excludes the family from every search, and `resonalyze_vc` reports such an edge as unverifiable rather than passing it. **BE/BW: 6-42 dB/oct; LR: 12/24/36 only (max 36** — an earlier "LR2-8" note was wrong; user-verified in PC-Tool 2026-07-13; the steepest available slope is BW/BE 42); HP/LP independent per channel; **corner frequency range 20–20480 Hz, steps in 1 Hz** — user-verified in PC-Tool (step 2026-08-22, range 2026-08-23), correcting an earlier "accuracy to 0.01 Hz" here which was wrong. ⚠️ **The SUB channel's UI offers only 20–300 Hz, and that is NOT a hardware limit — a higher corner can still be typed in by hand** (user, 2026-08-23). So 20–20480 is the range for every channel including the sub, and the 300 is an ADVISORY: what the UI offers by default, not what the processor accepts. Recording 300 as a bound in a machine-readable profile would make a tool refuse a legitimate sub crossover above it — the mirror of silently rounding an impossible value, and just as wrong. This is the founding case of the general **full-range rule** in the EQ row above — where a UI mode offers a subset, the profile carries the superset — so it is an instance, not an exception. No profile field for "possible but not offered" is needed once the bound is the full one; the 300 stays prose as guidance to a human, never as a limit. (The 0.01 Hz figure belongs to the parametric EQ's own frequency control, not to a crossover corner.) |
| Channel level (gain) | **−30 … +5 dB, the SAME range on BOTH tiers** — virtual channels and driver outputs alike (user-verified on his own Ultra S, 2026-08-23; relayed via the cockpit). ✅ **A typed gain is EXACT, and a re-upload changes nothing** (bench 2026-09-02, fact 4): −6.0 dB measures −5.9998 dB, flat to 0.021 dB across the band; re-uploading the whole configuration to the processor gives +0.0001 dB with 0.011 dB of spread — so a level shift between two captures is never the upload; look at the rig. ⚠️ **Not symmetric with the EQ row's `−30 … +12 dB`, which is BAND gain** — two different controls, and the +12 does not apply to a channel level. That asymmetry is why this had to be measured rather than inferred: a check that borrowed the EQ range for a channel trim would pass values the hardware refuses. Belongs in a project's machine-readable `dsp_profile.json` as `channel_gain.range_db` — `rew_tool/resonalyze_vc.py` reports `enterable: null` for every channel gain until it is there. **Still unmeasured and NOT to be guessed:** `parametric_eq.freq_range_hz` and `crossover_filters.corner_freq_range_hz`; this file gives only the 1 Hz step for each, and a plausible range written into a machine-readable profile becomes a limit code ENFORCES and nobody re-questions |
| Delays / polarity / phase | Delay per channel (ms, fine step), **up to 20.82 ms — on BOTH the driver (output) channels AND the virtual channels, and they SUM** (user-verified 2026-07-15) → enter a delay in ONE layer only, keep the other at 0. ✅ **A typed delay is quantised to a WHOLE SAMPLE at the processing rate** — 10.4167 µs at 96 kHz (bench 2026-09-02, fact 3): 0.05 ms = 4.800 samples lands on 5.0003, 0.32 ms = 30.720 on 30.9996, 1.00 ms = 96 on 95.9981. The published step "3.5 mm or 0.01 ms" is that figure rounded, and a model built on 0.01 ms drifts 4 % (0.4 mm per 10 mm of alignment); the settings sheet's **samples** column is the number the device holds. Rounding vs truncation upward is NOT separated by this data (both fractions measured sat above the half-sample). Polarity NORM/INV per channel; **Phase 0–354.375° in 5.625° steps** = **one RBJ 2nd-order all-pass, Q = 1.0000, whose corner the processor places so the phase equals the setting AT the channel's reference crossover — the configured LPF on a sub, the configured HPF otherwise (bypass and slope OFF keep the reference); capped at 3/16 of the rate = 18 kHz, so at a reference above 1 kHz the smallest steps collapse onto one filter** (5.625/11.25/28.125° at 5 kHz all deliver 29.5°). Measured 2026-09-02 (fact 6), modelled by `rew_tool/phase_rotation.py`, applied by `predict.py`; not visible in the EQ bank; exists on sub/mid/high channel types, not on `low`/`fullrange` (those get polarity only). Not a polarity flip (180° at 5 kHz is −23° at 1 kHz) and not free: the group delay lands BELOW the corner — 180° at a 500 Hz reference costs 640–690 µs over 20–100 Hz. Method: the midbass is the anchor you do not rotate; order midbass → sub → mid → tweeter (`helix-phase-allpass.md`) |
| Presets | Several; ⚠️ **switching can silently reset the INPUT to another card** — check the input after every switch (Pre-session #4) |
| Input stage — **OUT OF METHOD SCOPE** | The stage exists and is **not a tuning tier**. It forms the INPUT signal when the source is a factory head unit's **speaker-level** outputs; on optical, Bluetooth or USB it is not in the path at all (user, 2026-08-23). ⚠️ **What is scoped out is our WORK on it, not its existence** — recovering a flat input by undoing what a factory head unit already did (its EQ, its delays, its all-passes) is a different problem from tuning a system, and **this method does not solve it**. So: no ledger rows for this tier, and its absence from a ledger is not an incomplete project. Marked `in_scope: false` in the machine profile, which is why it stops appearing in `open-questions` — a list carrying two permanently unanswerable entries is a list people stop reading |
| Inputs | RCA/LineIN · optical S/PDIF · BT HD (module) · USB expansion cards; **ISA** (Input Signal Analyzer) = real-time clip check |
| Virtual routing / rear | Virtual-channel input mixer takes **signed percentages** → a true differential rear-fill feed works: Virtual C (Rear L) = In L **+50%** / In R **−50%**, Virtual D mirrored (verified 2026-07-15: correlated pink → SILENCE from the rears). **RearRC** (Conductor remote knob) = live rear-level control on the `[RearATT]`-tagged virtuals — a ready-made ear-ladder for rear-fill level. Whole-OUTPUT polarity INV can NOT build a differential (it inverts the sum, not one mixer leg) |
| Gain staging | **Method (universal):** min amp gain + max DSP level → raise to the first THD jump (RTA) → back off ~10%. ⚠️ **`−6 dB` Output is RIG-specific, NOT a default:** it follows from the Ultra S's 8 V outputs + the sensitivity of the author's SPECIFIC amps (≈4 V). A different DSP / different amps → a different number; derive it by YOUR own measurement, don't copy −6 dB blindly |
| RTC & remote knobs | **RTC (Remote Tone Control) is GLOBAL — preset switching does NOT change it**, and Director/Conductor knob positions are **NOT stored in `.pct6` backups** → the "competition state" = preset + a manual pre-judging checklist (RTC state, sub-knob position, rear on/off). RTC anchor values are interpolation endpoints deliberately outside the usable master window — evaluate APPLIED values inside the window, not anchors. Foolproof config that worked: shape the RTC curve so a chosen master level is its flat point, park the master there, control loudness upstream (source level) — disabling RTC needs the PC-Tool connection. Measurement hygiene: **RTC OFF on every measurement series** (constant-but-unknown RTC kills absolute-shape validity; differential deltas survive, `diagnostic §30`) |
| Native rate | **96 kHz** — measure the main mic rig at it |
| Config file | `.pct6` — **encrypted** (RESTORE-only, can't be parsed). ✅ Proven 2026-06-14: `AT` header + entropy 7.92 b/byte, 0 archive magic, no decompressor; two versions differing by a few parameters → a shared prefix of only **3 bytes** + 2% matches (random level) = a cipher with a per-save IV, NOT an archive/compression. Not reversible. → a backup map is mandatory (`naming-and-structure.md §4a`); the PC-Tool file version ≠ our `vN`. **The only readable path is via the EQ export (text, Audiotec-Fischer, `helix-eq-export.md`)** — and even that is only the EQ bank, not crossovers/TA/gain/routing (those come from measurement OR a **screen-read off the PC-Tool screen**, `references/tooling/screen-read-dsp.md`) |
| Software | PC-Tool is **Windows-only** (on Mac → Parallels; the courier step = the EQ export via a shared folder) |
| Other | **AKM DAC filter** (ACO Features → AKM Config): SQ recommendation **Short Delay Slow Roll Off** — *Short Delay* = minimum-phase → no pre-ringing → a tighter attack; *Slow Roll-Off* = gentle → less ringing (a tiny roll-off near Nyquist, unimportant). This is taste/SQ-philosophy, not dogma. ⚠️ **Changing the DAC filter → do NOT redo TA** (global + uniform across all channels → relative timing intact; the time effect is near Nyquist ~20k+, not in the joint band) — just an aural sanity-check of transients. FX/Virtual X affects the measurement (the `FX` suffix in names) |

## EQ transfer — two paths

1. **Canonical (Helix): file import** of Audiotec-Fischer → `helix-eq-export.md`. Advantage: in REW you immediately see the filters' effect on the curve. ⚠️ If file import is inconvenient (e.g. the path through Parallels/VM on Mac — feedback from a live test) → you can try path 2 on the **Helix PC-Tool window** too (the keystroke approach is generic; PC-Tool field compatibility is **provisional, not verified on Helix**).
2. **Alternative (and the path for DSPs without import): REW-EQ-CopyPaste-Assistant** (github.com/IvanBakhmutov/REW-EQ-CopyPaste-Assistant) — a Windows/PowerShell helper: "Copy" in REW's EQ section → the tool converts the clipboard into keystroke sequences and types the bands into the DSP software's window itself (click the first band → auto-entry). **30+ platforms**: Musway TUNEST_PC (verified by the skill author), ESX, Zapco, Goldhorn, Ground Zero, Nakamichi-K, etc. Claude can prepare a filter package for copying the same way it prepares an Audiotec-Fischer file (via REW or `rew_tool`).

## Worked settings-sheet example (illustrates SKILL.md's presentation rule)

`SKILL.md`'s always-loaded core requires every actionable DSP change to be presented as a step-by-step, arrow-pointer (`──►`) list — samples **and** ms, never a bare file reference. ⚠️ **Gains and parameters: ABSOLUTE target values only** — never relative phrasing like "remove the +3" (a live sheet with "gain −3.0 (remove the historical +3)" was read as *change by −3* and landed 3 dB off the intent; hardware is truth, but the sheet caused the fork). This is what that looks like on THIS DSP (a different DSP profile would show its own PC-Tool/app steps; the rule itself is DSP-agnostic — see SKILL.md).

```markdown
### Крок 1. Налаштування процесора в Helix PC-Tool (Примітка: затримки розраховано для Helix 96 кГц)
1. Запустіть Helix PC-Tool на вашому Windows-комп'ютері або у віртуальній машині.
2. Перейдіть у меню Crossovers (Кросовери) та встановіть для каналів:
    • ВЧ (tw-L, tw-R): HPF = 3500 Гц | Крутизна = Linkwitz-Riley 24 дБ (LR4).
    • СЧ (m-L, m-R): HPF = 300 Гц, LPF = 3500 Гц | LR4 (24 дБ).
    • НЧ (w-L, w-R): HPF = 60 Гц, LPF = 300 Гц | LR4 (24 дБ).
    • Сабвуфер (sw): HPF (Subsonic) = 20 Гц BW2 (Butterworth 12 дБ) | LPF = 60 Гц LR4 (24 дБ).
    • Тил (r-L, r-R) та Центр (c): Тимчасово заглушіть (Mute).
3. Перейдіть у меню Time Alignment (Затримки) та введіть значення (мілісекунди — абсолютний референс, семпли наведено для 96 кГц):
    • tw-L (Канал A) ──► 626 семплів (6.52 мс)
    • tw-R (Канал B) ──► 510 семплів (5.31 мс)
    • m-L  (Канал C) ───► 633 семпли (6.59 мс)
    • m-R  (Канал D) ───► 515 семплів (5.36 мс)
    • w-L  (Канал E) ───► 522 семпли (5.44 мс)
    • w-R  (Канал F) ───► 450 семплів (4.69 мс)
    • sw   (Канал H) ────► 0 семплів (0.00 мс)
4. Перейдіть у меню Gain (Рівні) та притисніть ліву сторону для базового балансу сцени:
    • tw-L ──► -1.5 дБ | m-L ──► -2.0 дБ | w-L ──► -1.5 дБ
    • Всі праві канали та саб залишаються на 0.0 дБ.
5. Збережіть цей пресет у процесор (наприклад, у Слот 1) та збережіть файл на комп'ютер як B8_EMMA_v1_foundation.pct6.
```

## Where the bench numbers live (2026-08-31 … 2026-09-04)

Every ✅ above that names a **fact N** comes from one published place:
`ayukhno/autosound-measurements`, `hardware/helix/dsp-ultra-s/` (CC BY 4.0) — `FACTS.md` (nine
facts, each with the files that decided it), `measurements/*.txt` (109 curves, `frequency_Hz<TAB>dB
<TAB>phase_deg`, 1/96 octave anchored at 20 Hz, 957 points, unsmoothed), the manifests
(`bench3-manifest.json`, `phase-turns-manifest.json`, `bw24-manifest.json`) and
`measurements/README.md`, which documents every defect in the data in place. **Everything was
measured electrically** — processor output straight into the interface, no microphone, no cabin —
which is the right shape when the question is the DSP's arithmetic rather than a driver.

**The one rule for using them:** every group carries its own reference capture, and only ratios
taken within a group mean anything. And a residual carries its **band, mask and grid** or it is not
a number (`diagnostic-techniques.md` §35): the same BW24 residual is 0.443° on the 1/96-octave grid
and 0.220° on REW's raw linear bins.

The subset this tree runs its selftests against — BW24/LR24 at 460 Hz, BE36 at 1 kHz, LR36 at
8 kHz, and the 32 fitted phase-control cases — is copied verbatim into
`rew_tool/testdata/helix-bench/` (its `README.md` says what each set decides). The machine-readable
half of this file is `profiles/audiotec-fischer-helix-dsp-ultra-s.json` (`phase_control`,
`delay.sample_quantised*`, `crossover_filters.verification_note`), synced FROM here.

Still open, and cheap to close on the bench: whether the delay quantiser rounds or truncates (one
capture with a fraction below the half-sample), and whether mid/high channels step the phase
control by 5.625° or 11.25° (one step on a mid/high channel at a reference below 1 kHz). Both are
in the profile's `_open_questions`.
